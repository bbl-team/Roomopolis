Changelog BBL Rooms

Placer Item, on shift right click show all option, (any saved structures)

# 4.0.16
- Changed saving and syncing to use the location of the template file instead of the temple id allowing for the same template to be configured differently in jsons
- This change does not require changes to existing templates

# 4.0.15
- Templates now respect original blockstates when the palette is changed 

# 4.0.14
- Changed error message for placer tooltip when it hasnt been used yet 

# 4.0.13
- Fixed json using pallets instead of palettes
- Added full wiki

# 4.0.12
- Fixed Creative Mode translation 

# 4.0.11
- Placer will now always open on shift right-click
- Fixed some more translation issues

# 4.0.10
- Placer screen now always opens on shift right click even when looking at a block

# 4.0.9
- Added "placer_position", defaults to 1, this is the order of the structures inside the placer screen, lowest numbers first
- Palette selection button no longer shows if the show_in_placer option is false
- Palette results now have a sort bar 

# 4.0.8
- Template palettes are now placed as the structure instead of being replaced after placement
- Improved palette screen to allow for more palette options to be seen
- Placer now has a texture

# 4.0.7
- Removed corridor test

# 4.0.6
- Fixed door defaults having some values set to 1, no values mean no door opening will be created

# 4.0.5
- Fixed some translations
- Fixed tooltip overriding structures
- show_in_placer now defaults to true
- blocks_required now defaults to true
- Fixed showInPlacer still showing sturctures in the placer screen
- Improved most screen stuff to make it cleaner
- Moved all templates into "template_ignore" folder to prevent them from being loaded in preparation for modpacks

# 4.0.4
- Structure s with more thna 2000 block no longer render on placer screen
- will still render when clicked
- added show_in_placer option default true, if false will not show the structure in the placer screen or template screen!

# 4.0.3
- Renamed "flags" to "restriction_options"
- Renamed "placement" to "placement_options"
- Added "item_required" option to placement options, this basically locks a structure behind an item that is consumed when the structure is consumed 

# 4.0.2
- Added palette system, allows template to have optional palettes which can be used to change teh structure appearance 
- Move improvements to the placer item 

# 4.0.1
- Greatly improved added new screens and better readability  

# 4.0.0
- DO NOT UPDATE TO THIS VERSION THIS IS STILL WIIPPPP
- This is a complete overhaul of the mod, every aspect of the mod has changed 
- Removed all blocks
- Removed KeyItem class
- Added Placer Item, shift right click to open a menu showing all valid structures. Click on a structure to select it for placement 
- Re done networking

# 3.1.0
- Name changed from Roomopolis to BBL Rooms
- Re enabled block rendering preview 
- Removed Core as dependency
- Added new auto updating

# 3.0.0
- Updated to minecraft 26.1 snapshots
- Reworked Key Item basically a lot easier to create custom keys for mods
- Most key fields are optional now
- Added API for the KeyItemBuilder
- Added .rotation, this allow structures to be rotated in any direction respecting the rotation of the direction of the player when placing the key, this is optional and defaults to none
- Deprecated all blocks for removal in official 26.1 update
